name: "Semantic Version Bumper for npm"
description: "coming soon..."
inputs:
  github_token:
    description: "GitHub access token"
    required: true
    default: ${{ github.token }}
runs:
  using: node16
  main: "dist/index.js" 